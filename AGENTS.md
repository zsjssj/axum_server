# 项目协作规范

本文件适用于仓库内的全部目录和文件。

## 注释语言

- 新增或修改的代码注释必须使用中文，包括行注释、块注释、文档注释和模块级文档注释。
- 注释中的代码标识符、协议字段、配置键、库名称和通用技术术语可以保留英文原文。
- 引用外部规范或必须与外部系统完全一致的文本时，可以保留原文，并在必要时补充中文说明。
- 注释应解释设计意图、约束、边界条件或不直观的实现原因，不应只是逐句复述代码。
- 修改含有非中文注释的代码时，应在不改变原意的前提下同步将相关注释调整为中文。

## 架构与文档

- 保持 `Handler → Service → Repository → Database` 的依赖方向，禁止下层反向依赖上层。
- 路由、配置、模块边界或关键设计发生变化时，同步更新 `docs/ARCHITECTURE.md` 和相关 API 文档。
- 数据库结构变更通过新增迁移脚本完成，不修改已经发布或可能已执行的迁移文件。
